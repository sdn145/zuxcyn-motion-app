function addKeyframe(){const l=Zuxcyn.activeLayer;if(!l)return toast("Pilih layer dulu");commit();Zuxcyn.keyframes.push({time:Zuxcyn.currentTime,layerId:l.id,x:l.x,y:l.y,scale:l.scale,rotation:l.rotation,opacity:l.opacity});Zuxcyn.keyframes.sort((a,b)=>a.time-b.time);renderKeyframes();toast("Keyframe ditambahkan")}
function renderKeyframes(){const b=$("keyframeTrack");b.innerHTML="";const d=Zuxcyn.duration||5;Zuxcyn.keyframes.forEach(k=>{const dot=document.createElement("div");dot.className="key-dot";dot.style.left=(k.time/d*100)+"%";dot.title=formatTime(k.time);dot.onclick=()=>seekTo(k.time);b.appendChild(dot)})}
function applyKeyframeAtTime(t){const l=Zuxcyn.activeLayer;if(!l)return;const ks=Zuxcyn.keyframes.filter(k=>k.layerId===l.id);if(!ks.length)return;let a=ks.filter(k=>k.time<=t).pop(),b=ks.find(k=>k.time>t);if(!a){a=ks[0];b=ks[0]}if(!b||b.time===a.time){Object.assign(l,a);refreshStage();return}const p=(t-a.time)/(b.time-a.time);["x","y","scale","rotation","opacity"].forEach(q=>l[q]=a[q]+(b[q]-a[q])*p);refreshStage()}
function updateKeyframeAnimation(){if(Zuxcyn.keyframes.length)applyKeyframeAtTime(Zuxcyn.currentTime)}
document.addEventListener("DOMContentLoaded",()=>{$("addKeyframeBtn").onclick=addKeyframe;renderKeyframes()});
